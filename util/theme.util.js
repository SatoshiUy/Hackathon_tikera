import { createTheme } from '@mui/material';

export const lightTheme = createTheme({ palette: {},
  typography: {
    fontFamily: [
      'Oswald','sans-serif'
    ].join(','),
    fontSize: 16,
  }
});
export const darkTheme = createTheme({ palette: {},
  typography: {
    fontFamily: [
      'Oswald','sans-serif'
    ].join(','),
    fontSize: 16,
  }
});

